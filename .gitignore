# Firebase build and deployment files
/firebase-debug.log
/firebase-debug.*.log
.firebaserc

# Firebase Hosting
/firebase.json
*.cache
hosting/.cache

# Firebase Functions
/functions/node_modules/
/functions/.env
/functions/package-lock.json

# Firebase Emulators
/firebase-*.zip
/.firebase/
/emulator-ui/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (local configs)
/.env.*

# Node dependencies
/node_modules/
/server/node_modules/

# Server runtime data (SQLite DB + uploads) and test data
/server/data/
/server/.testdata/
/.pwtest-data/

# Playwright test artifacts
/playwright-report/
/test-results/

# Claude Code / project memory
# (.claude contents are local, EXCEPT skills — those are shared team tooling)
/.remember/
/.claude/*
!/.claude/skills/
